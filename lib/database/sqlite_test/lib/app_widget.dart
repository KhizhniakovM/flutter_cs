import 'package:flutter/material.dart';
import 'package:sqlite_test/pages/home_page.dart';

class TestApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
